<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GoCart - Smart Shopping</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <style>
        /* --- CSS Variables for Easy Customization --- */
        :root {
            --primary-color: #0077b6; 
            --secondary-color: #00b4d8; /* A vibrant accent */
            --dark-text: #343a40;      /* Dark grey for text */
            --light-bg: #fdfdfd;       /* Clean, off-white background */
            --primary-font: 'Poppins', sans-serif;
        }

        body, html {
            height: 100%;
            margin: 0;
            font-family: var(--primary-font);
            overflow-x: hidden; /* Prevents horizontal scroll */
        }

        .landing-container {
            display: flex;
            min-height: 100vh;
            width: 100vw;
        }

        .content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-start; /* Align text to the left for a cleaner look */
            background: var(--light-bg);
            text-align: left;
            padding: 5rem;
        }

        .content .icon {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }

        .content h1 {
            font-size: 4rem;
            font-weight: 700;
            color: var(--dark-text);
            margin-bottom: 0.5rem;
        }

        .content p {
            font-size: 1.25rem;
            margin: 20px 0 30px 0;
            color: #6c757d; /* Softer text color */
            max-width: 500px; /* Improve readability */
        }

        .button-group {
            display: flex;
            align-items: center;
            flex-wrap: wrap; /* Allows buttons to wrap on smaller screens */
            gap: 1rem; /* Adds space between items */
        }

        .content .btn-custom {
            font-size: 1.1rem;
            font-weight: 600;
            padding: 15px 35px;
            border-radius: 50px;
            background-color: var(--primary-color);
            border: 2px solid var(--primary-color);
            color: white;
            transition: all 0.3s ease-in-out;
            box-shadow: 0 4px 15px rgba(0, 119, 182, 0.3); /* Subtle shadow for depth */
        }

        .content .btn-custom:hover {
            transform: translateY(-3px); /* Lifts the button on hover */
            box-shadow: 0 7px 20px rgba(0, 119, 182, 0.4);
            background-color: var(--secondary-color);
            border-color: var(--secondary-color);
        }

        .content .btn-outline-custom {
            font-size: 1.1rem;
            font-weight: 600;
            padding: 15px 35px;
            border-radius: 50px;
            background-color: transparent;
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            transition: all 0.3s ease-in-out;
        }

        .content .btn-outline-custom:hover {
            transform: translateY(-3px);
            background-color: var(--primary-color);
            color: white;
            box-shadow: 0 7px 20px rgba(0, 119, 182, 0.2);
        }

        /* --- 🎨 CSS CHANGE 1: UPDATED .image-side styles --- */
        .image-side {
            flex: 1.2;
            clip-path: polygon(25% 0, 100% 0, 100% 100%, 0% 100%);
            position: relative; /* Required for positioning the video */
            overflow: hidden; /* Hides parts of the video that are outside the container */
        }
        
        /* --- 🎨 CSS CHANGE 2: NEW styles for the background video --- */
        .background-video {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover; /* This makes the video cover the container, like background-size: cover */
        }
        
        .modal-body .ratio {
            overflow: hidden;
            border-radius: 0.5rem;
        }

        @media (max-width: 768px) {
            .landing-container {
                flex-direction: column;
            }

            .image-side {
                flex: 0.8;
                min-height: 300px;
                clip-path: none;
            }

            .content {
                align-items: center;
                text-align: center;
                padding: 2.5rem;
            }

            .content h1 {
                font-size: 3rem;
            }

            .button-group {
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="landing-container">
        <div class="content">
            <div class="icon">
                <i class="fa-solid fa-cart-shopping"></i>
            </div>
            <h1>SCHNEL</h1>
            <h1>GoCarts</h1>
            <p>Smart shopping made simple. Your groceries, delivered faster, easier, and better than ever before.</p>
            <div class="button-group">
                <a href="/listings" class="btn btn-custom">Start Shopping <i class="fa-solid fa-arrow-right ms-2"></i></a>
                <a href="#" class="btn btn-outline-custom" data-bs-toggle="modal" data-bs-target="#demoModal">
                    Watch a Demo <i class="fa-solid fa-play ms-2"></i>
                </a>
            </div>
        </div>

        <div class="image-side">
            <video playsinline autoplay muted loop class="background-video">
                <source src="Cart.mp4" type="video/mp4">
                Your browser does not support the video tag.
            </video>
        </div>
    </div>

    <div class="modal fade" id="demoModal" tabindex="-1" aria-labelledby="demoModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content" style="background: #000; border: none;">
                <div class="modal-header" style="border-bottom: none;">
                    <h5 class="modal-title text-white" id="demoModalLabel">GoCart Demo</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="ratio ratio-16x9">
                        <iframe id="demoVideo" src="https://www.youtube.com/embed/CU53EbSEVAA" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const demoModal = document.getElementById('demoModal');
        const video = document.getElementById('demoVideo');
        
        demoModal.addEventListener('hidden.bs.modal', function (event) {
            const originalSrc = video.src;
            video.src = '';
            video.src = originalSrc;
        });
    </script>
</body>
</html>